package swiftcards.core.game.activities;

import swiftcards.core.game.GameActivity;

public class CardsToTakeIncreased extends GameActivity<Integer> {
    public CardsToTakeIncreased(int totalCardsToTakeAmount) {
        super(totalCardsToTakeAmount);
    }
}
