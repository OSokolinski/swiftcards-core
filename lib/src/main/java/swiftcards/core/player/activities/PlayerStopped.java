package swiftcards.core.player.activities;

import swiftcards.core.player.PlayerActivity;

public class PlayerStopped extends PlayerActivity<Integer> {
    public PlayerStopped(int playerId) {
        super(playerId);
    }
}
