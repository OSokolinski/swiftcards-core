package swiftcards.core.player.activities;

import swiftcards.core.player.PlayerActivity;

public class PlayerTurnPending extends PlayerActivity<Integer> {
    public PlayerTurnPending(int playerId) {
        super(playerId);
    }
}
